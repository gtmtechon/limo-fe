import { createStore } from 'vuex';

export default createStore({
  state: {
    // 여기에 전역 상태를 정의합니다.
  },
  getters: {
    // 여기에 상태를 가져오는 게터를 정의합니다.
  },
  mutations: {
    // 여기에 동기적인 상태 변경 로직을 정의합니다.
  },
  actions: {
    // 여기에 비동기적인 작업과 뮤테이션 커밋 로직을 정의합니다.
  },
  modules: {
    // 여기에 모듈을 정의합니다.
  },
});
